spring:
  datasource:
    url: jdbc:h2:mem:imobcontrol-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

imobcontrol:
  bootstrap-admin:
    name:
    email:
    password:
