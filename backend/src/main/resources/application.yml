spring:
  application:
    name: imobcontrol-api

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never

info:
  app:
    name: ImobControl API
    description: Back-end Java do sistema ImobControl
